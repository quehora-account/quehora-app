plugins {
    id "com.android.application"
    id 'com.google.gms.google-services'
    id 'com.google.firebase.crashlytics'
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def mapboxToken = localProperties.getProperty('MAPBOX_DOWNLOADS_TOKEN')

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')
if (flutterVersionCode == null) {
    flutterVersionCode = '1'
}

def flutterVersionName = localProperties.getProperty('flutter.versionName')
if (flutterVersionName == null) {
    flutterVersionName = '1.0'
}

//def keystoreProperties = new Properties()
//def keystorePropertiesFile = rootProject.file('key.properties')
//if (keystorePropertiesFile.exists()) {
//    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
//}

android {
    compileSdkVersion 34
    namespace "app.hoora"
//    compileSdk flutter.compileSdkVersion
    ndkVersion flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    defaultConfig {
        applicationId "app.hoora"
        minSdkVersion 23
        ndkVersion = "27.0.12077973"

        targetSdkVersion flutter.targetSdkVersion
        versionCode 51
        versionName "1.16.0"
        multiDexEnabled true
    }

    signingConfigs {
        release {
            keyAlias "upload"
            keyPassword "Dhoora=app4"
            storeFile file("keystore1.jks")
            storePassword "Dhoora=app4"
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }

    flavorDimensions "app"

    productFlavors {
        production {
            dimension "app"
            applicationId "app.hoora"
            resValue "string", "app_name", "Quehora"
        }
        staging {
            dimension "app"
            applicationId "app.hoora.staging"
            resValue "string", "app_name", "Quehora (Staging)"
        }
    }
}

flutter {
    source '../..'
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
}
// Ajoutez cette ligne à la fin du fichier
apply plugin: 'com.google.gms.google-services'
